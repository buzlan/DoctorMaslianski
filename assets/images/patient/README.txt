Doctor Maslianski — patient UI assets in use

Runtime:
- plant_progress.png   → Today progress card decoration
- splash_logo.png      → Expo native splash (centered logo, contain)
- splash_screen.png    → full-screen design reference (not used by splash plugin)
- app_icon_primary.png → Expo app icon (iOS + Android adaptive foreground)

Progress ring is drawn with Views (no progress_*.png).
