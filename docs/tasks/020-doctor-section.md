# TASK-020 — Doctor section (mock)

Status: NOT STARTED

Milestone: Doctor section (mobile, mock)

## Goal

**Doctor section:** clinic/doctor info, upcoming appointments, and doctor-defined instructions as display text from mock data.

This task names a **section**, not a guaranteed tab item.

## Why this task is needed

Doctor is a planned patient destination. There is still no chat backend. Primary navigation should preferably remain around five sections (Today, Treatment, Diary, Activity, Doctor) rather than six.

## Dependencies

- TASK-019 (daily companion loop exists)
- Placement is **not** predetermined

## Requirements

- Display-only.
- Communication entry points may be tel/mailto or “coming later”, not a custom messenger.
- **Plan Mode for this task decides navigation placement** based on the shell that exists then (likely Today, Treatment, Diary, Activity).
- Prefer adding Doctor as the fifth primary section if that still keeps the bar compact.
- Do **not** assume a sixth tab.
- Do **not** add a Photos tab as a side effect.
- Do not generate medical advice.

## Out of scope

- Doctor dashboard
- Messaging product
- Changing appointments on a server
- Prescribing a Photos tab
- Hard-coding “sixth tab”

## Expected files or areas affected

- `src/modules/doctor/**`
- Routes/layout only as Plan Mode specifies

## New dependencies

No.

## Plan Mode

Yes (new module + **navigation placement**).

## Acceptance criteria

- Doctor section is reachable.
- Content comes from mock data.
- No medical advice is generated by the app.
- Primary navigation stays compact (preferably five sections; Photos still not a top-level destination).
- The application remains runnable.

## Verification

```bash
npx tsc --noEmit
npm run lint
```

Manually verify on iOS Simulator and Android Emulator.
